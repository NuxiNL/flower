syntax = 'proto3';

package flower.protocol.egress;

message ConnectRequest {
  map<string, string> connection_labels = 1;
}

message ConnectResponse {
  fd server = 1;
}

service Egress {
  rpc Connect(ConnectRequest) returns (ConnectResponse);
}
