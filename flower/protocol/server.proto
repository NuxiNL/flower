syntax = 'proto3';

package flower.protocol.server;

message ConnectRequest {
  fd client = 1;
  map<string, string> connection_labels = 2;
}

message ConnectResponse {
}

service Server {
  rpc Connect(ConnectRequest) returns (ConnectResponse);
}
